---
description: Set this project up with the harness, or refresh what the harness owns after its choices changed.
---

Use the `harnex-setup` skill to set up the project in `${CLAUDE_PROJECT_DIR}`.

Follow it exactly: ask what it says to ask, offer only what the harness holds, show the
plan the script prints before anything is written, and write only after an explicit yes.
