# macOS
.DS_Store

# Secrets and local overrides
.env
.env.*
CLAUDE.local.md
.claude/settings.local.json

# Python
.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
dist/

# Runtime state of a harnessed checkout
.harnex/

# Private working material, kept out of the public repository.
# Anchored to the root so same-named directories deeper in the tree
# (harness/context/openspec/, template/.claude/, ...) are still tracked.
/openspec/
/.claude/
/.agents/
/docs/
